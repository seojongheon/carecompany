# HTTP 요청 헤더 경계 계약

- `pnpm dev`와 `pnpm start`는 Node 요청 헤더 상한 65,536바이트로 Next.js를 실행한다.
- 32KB 단일 쿠키 헤더 요청은 HTTP 파서에서 거부되지 않는다.
- 70KB 단일 쿠키 헤더 요청은 HTTP 431로 거부된다.
- 정상 요청의 라우팅, Supabase 세션 갱신, 인증 권한 검사는 기존 계약을 유지한다.
